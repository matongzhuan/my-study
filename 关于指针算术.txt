对指针加1，增加的量等于它指向的类型的字节数。将指向double类型的指针加1后，则数值将增加8


通常情况下，C++将数组名解释为数组第一个元素的地址

数组名与指针的区别：
1、数组名是常量，指针是变量，可以对指针进行加减操作，但是不能对数组名进行
2、对数组名应用sizeof的时候，得到的是数组的长度，而对指针应用sizeof得到的是指针的长度。

如果给cout一个字符的地址，则它将从该字符开始打印，直到遇到空字符为止