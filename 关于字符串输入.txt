面向行的输入：
cin.getline(name,size):获取的输入是传入到数组当中的，注意输入的字符不能超过size-1，由于字符串要以空字符作为结尾

cin.get(name,size):与getline类似，但是不读取空字符，将其留在输入队列中
所以需要cin.get()来获取留下的空字符，保证程序正常


注意：当输入的字符串比分配的空间大的时候，多余的字符会留在输入队列中，并且getline（）会设置失效位。这个时候可以用cin.clear（）来清除输入队列